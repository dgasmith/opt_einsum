name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python >=3.9
  - pytorch-cpu >=2.0,<3.0.0a

    # Testing
  - autoflake
  - black 
  - codecov
  - isort
  - mypy
  - pytest
  - pytest-cov
