name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - numpy
  - nomkl

    # Backends
  - tensorflow-base
  - dask
  - sparse
  - theano
  - pytorch-cpu
  - autograd
  - jax
  - jaxlib

    # Testing
  - pytest
  - codecov
  - pytest-cov

