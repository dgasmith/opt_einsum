name: test
channels:
  - conda-forge
  - pytorch
dependencies:
    # Base depends
  - python
  - numpy
  - nomkl

    # Backends
  - tensorflow
  - dask
  - sparse
  - theano
  - pytorch-cpu
  - autograd

    # Testing
  - pytest
  - codecov
  - pytest-cov

  # non-conda backends
  - pip
  - pip:
    - jax
    - jaxlib
