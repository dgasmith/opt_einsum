name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python >=3.8
  - numpy >=1.23
  - nomkl

    # Backends
  - tensorflow-cpu
  - dask
  - sparse
  - pytorch-cpu
  - jax

    # Testing
  - codecov
  - mypy ==1.11*
  - pytest
  - pytest-cov
  - ruff ==0.5.*
