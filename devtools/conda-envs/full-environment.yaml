name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - numpy
  - nomkl

    # Backends
  - tensorflow-base
  - dask
  - sparse
  - pytorch-cpu
  - autograd
  - jax

    # Testing
  - autoflake
  - black
  - codecov
  - isort
  - mypy
  - pytest
  - pytest-cov
