name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - numpy
  - nomkl

    # Backends
  - tensorflow ==2.10.*
  - dask ==2021.*

    # Testing
  - autoflake
  - black
  - codecov
  - isort
  - mypy
  - pytest
  - pytest-cov
