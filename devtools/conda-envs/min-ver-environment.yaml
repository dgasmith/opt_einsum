name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python >=3.8
  - numpy >=1.23
  - nomkl

    # Backends
  - tensorflow-cpu ==2.10.*
  - dask ==2021.*

    # Testing
  - codecov
  - mypy
  - pytest
  - pytest-cov
  - ruff ==0.5.*
