name: test
channels:
  - conda-forge
  - pytorch
dependencies:
    # Base depends
  - numpy
  - nomkl

    # Backends
  - tensorflow
  - dask
  - sparse
  - theano
  - pytorch-cpu

    # Testing
  - pytest
  - codecov
  - pytest-cov
