name: test
channels:
  - conda-forge
  - pytorch
dependencies:
    # Base depends
  - python
  - numpy
  - nomkl

    # Backends
  - tensorflow
  - dask
  - sparse
  - theano
  - pytorch-cpu
  - autograd

    # Testing
  - pytest
  - codecov
  - pytest-cov

   # Linting
  - flake8
  - flake8-spellcheck

  # non-conda backends
  - pip
  - pip:
    - jax
    - jaxlib
